# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools
name: Deploy to Firebase Hosting on merge

on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4  # Fetch code from repository

      - name: Install dependencies (**NEW STEP**)
        run: npm install  # Installs dependencies from package.json

      - name: Build the app
        run: npm run build

      - uses: FirebaseExtended/action-hosting-deploy@v0  # Deploy to Firebase
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_LONGDINO_FA749 }}
          channelId: live
          projectId: longdino-fa749

